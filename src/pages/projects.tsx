import React from "react";

import Layout from "../components/Layout";

export default () => {
    return (
        <Layout activeNavItem="projects">
            <h1>Hello World</h1>
        </Layout>
    );
};
